pipeline{
    agent any
    stages{
        stage("build"){
            steps{
                sh 'pwd'
                sh 'ls -la'
                dir('jenkins3') {
                    sh 'ls -la'
                    sh 'docker build -t movies-jenkins:v1 .'
                }
            }
            post{
                always{
                    sh "rm -f Dockerfile "
                }
                success{
                    echo "well done"
                }
                failure{
                    echo "ooooops"
                }
            }
        }
        stage("test"){
            steps{
                sh 'docker run -d -p 8090:80 movies-jenkins:v1 ' // run the contaienr on port 8090
                sh 'docker ps'
                }
            }
    }
    post{
        always{
            sh 'echo hello'
        }
        success{
            echo "========pipeline executed successfully ========"
        }
        failure{
            echo "========pipeline execution failed========"
        }
    }
}